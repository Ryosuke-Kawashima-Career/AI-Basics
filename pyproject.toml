[project]
name = "ai-basics"
version = "0.1.0"
description = "AI Basics Project"
authors = [
    { name = "Developer" }
]
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "accelerate",
    "datasets>=4.5.0",
    "matplotlib",
    "transformers",
]
